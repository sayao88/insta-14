
<div class="container">
  	<div class="wrapper col-md-8 col-md-offset-2 col-sm-10 col-sm-offset-1">
      	<% if notice then %>
    		<p class="text-center"><%= notice %></p>
      	<% end %>
		<h1>投稿一覧</h1>
        <div class="layout-btn vr-right">
            <%= link_to "New Photo", new_picture_path, class: 'mod-btn-default vr-width-auto vr-small' %>
        </div>
		<%= render 'picture_list' %>
	</div>
</div>

  <div id="page-top-btn" class="page-top-btn visible-place-200">
      <p><a class="move-page-top" id="move-page-top">▲</a></p>
  </div>